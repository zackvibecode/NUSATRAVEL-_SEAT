APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=sqlite
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=laravel
# DB_USERNAME=root
# DB_PASSWORD=

# Production (Render + Neon): set these on the host, not locally
# DB_CONNECTION=pgsql
# DATABASE_URL=postgresql://USER:PASSWORD@HOST/neondb?sslmode=require
# DB_URL="${DATABASE_URL}"
# DB_SSLMODE=require

SESSION_DRIVER=database
# 43200 minutes = 30 days. Sessions stay alive until the user logs out.
SESSION_LIFETIME=43200
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

# Hermes / Dropbox import API (Bearer token). Generate a long random string for production.
IMPORT_API_TOKEN=

# Google OAuth sign-in. Create credentials at https://console.cloud.google.com
# (APIs & Services > Credentials > OAuth Client ID > Web application).
# Redirect URI to register: {APP_URL}/auth/google/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI="${APP_URL}/auth/google/callback"

# Capacity alert email (daily digest of nearly-full trips)
SEATWEB_ALERT_THRESHOLD=5
SEATWEB_ALERT_DAYS_AHEAD=30

# Where backup success/failure notifications are sent
SEATWEB_BACKUP_MAIL_TO=

# Optional: encrypt backup archives
BACKUP_ARCHIVE_PASSWORD=
